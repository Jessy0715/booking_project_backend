# 複製成 .env 後填入真實值。.env 不進 git，.env.example 才進。

# 圖片存哪：local（預設，可不填）或 cloudinary
UPLOAD_STORAGE=local

# 從 Cloudinary 後台 API Keys 頁面複製；格式 cloudinary://<API Key>:<API Secret>@<Cloud name>
CLOUDINARY_URL=

# Claude API key，從 console.anthropic.com 取得
ANTHROPIC_API_KEY=
