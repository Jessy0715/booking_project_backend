# 複製成 .env 再依環境修改。.env 不會進版控。
# 本機開發不設也能跑 —— application.properties 與 docker-compose.yml 都有預設值。

POSTGRES_DB=booking
POSTGRES_USER=booking
POSTGRES_PASSWORD=booking

DB_URL=jdbc:postgresql://localhost:5433/booking
DB_USERNAME=booking
DB_PASSWORD=booking
